// start slingin' some d3 here.
$(document).ready(function() {
  var height = 960;
  var width = 500;

  var svg = d3.select('body').append('svg')
              .attr('width', width)
              .attr('height', height);
});